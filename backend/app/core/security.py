"""Security & Cryptography Module

Purpose:
    Handles HMAC-SHA256 signature generation and timing-safe verification,
    request timestamp validation against replay attacks, repository fingerprinting,
    and FastAPI security dependencies for protected routes.
"""
